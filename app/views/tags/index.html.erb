<h1>Listing tags</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%= tag.id %></td>
    <td><%= tag.name %></td>
    <td><%= link_to 'Show', game_tag_path(@game,tag) %></td>
    <td><%= link_to 'Edit', edit_game_tag_path(@game,tag) %></td>
    <td><%= link_to 'Destroy', game_tag_path(@game,tag), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tag', new_game_tag_path(@game) %>
