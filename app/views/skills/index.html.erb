<h1>Skills</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Created at</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @skills.each do |skill| %>
      <tr>
        <td><%= link_to skill.name, game_skill_path(@game,skill) %></td>
        <td><%= skill.created_at %></td>
        <td>
          <%= link_to 'Edit', edit_game_skill_path(@game,skill), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', game_skill_path(@game,skill), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New', new_game_skill_path(@game), :class => 'btn btn-primary' %>
