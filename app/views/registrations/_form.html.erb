<%= form_for [@game, @registration] do |f| %>
  <% if @registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% @registration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.number_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :character_id %><br />
    <%= f.number_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :paid %><br />
    <%= f.check_box :is_paid %>
  </div>
  <div class="field">
    <%= f.label :present %><br />
    <%= f.check_box :is_present %>
  </div>
  <div class="field">
    <%= f.label :setup_cleanup %><br />
    <%= f.check_box :setup_cleanup %>
  </div>
  <div class="field">
    <%= f.label :cp_game_id %><br />
    <%= f.number_field :cp_game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
